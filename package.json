{
  "name": "kiroku-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "npx tailwindcss -i src/styles/tailwind-input.css -o assets/styles.css --minify --content ./index.html"
  }
}